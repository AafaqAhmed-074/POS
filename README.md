This is a web application where a user can manage inventory, has a feature of letting the user to scan the barcode, can track total sales,
can add stock, can print receipts both using thermal and normal printer like hp printer.
